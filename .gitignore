# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db

# Raw PSP XLS download cache (parsed output goes to data/history/, which IS tracked)
cache/

# NOTE: data/ is intentionally NOT ignored — the collected dataset is the point.
